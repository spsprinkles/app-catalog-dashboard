define([], function () {
  return {
    "ConfigFieldUrlLabel": "The relative url to the JSON configuration for this application.",
    "ConfigWebUrlLabel": "The url to the site containing the configuration file. (The current site will be used if blank)"
  }
});